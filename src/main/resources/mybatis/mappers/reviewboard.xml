<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
      PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
   "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mapper.Reviewboard">
	<resultMap type="reviewboardVO" id="reviewboardResult">
		<result property="num" column="num" />
		<result property="nickname" column="nickname" />
		<result property="thumbnail" column="thumbnail" />
		<result property="viewcount" column="viewcount" />
		<result property="likecount" column="likecount" />
		<result property="writedate" column="writedate" />
		<result property="title" column="title" />
		<result property="content" column="content" />
		<result property="communityname" column="communityname" />
		<result property="introduction" column="introduction" />
		<result property="country" column="country" />
		<result property="area" column="area" />
		<result property="hashtag" column="hashtag" />
	</resultMap>
	
	<!-- 전체 게시글 조회 -->
	<select id="allBoardList" parameterType="hashmap" resultMap="reviewboardResult">
	<choose>
		<when test="consort != '국적'">
			select * from reviewboard where country=#{consort} and title like #{search} order by 
		</when>
		<otherwise>
			select * from reviewboard where title like #{search} order by 
		</otherwise>
	</choose>
	<choose>
		<when test="sort == 'viewcount'">
		viewcount desc, writedate desc 
		</when>
		<when test="sort == 'writedate'">
		writedate desc 
		</when>
		<when test="sort == 'likecount'">
		likecount desc, writedate desc 
		</when>
		<otherwise>
		writedate desc 
		</otherwise>
	</choose>
	limit #{pagefirst},#{pagesize};
	</select>
	
	<!-- 정렬순 전체 게시글 조회 -->
	<select id="SortList" parameterType="hashmap" resultMap="reviewboardResult">
	<choose>
		<when test="consort != '국적'">
			select * from reviewboard where country=#{consort} and title like #{search} order by 
		</when>
		<otherwise>
			select * from reviewboard where title like #{search} order by 
		</otherwise>
	</choose>
	<choose>
		<when test="sort == 'viewcount'">
		viewcount desc, writedate desc 
		</when>
		<when test="sort == 'writedate'">
		writedate desc 
		</when>
		<when test="sort == 'likecount'">
		likecount desc, writedate desc 
		</when>
		<otherwise>
		writedate desc
		</otherwise>
	</choose>
	limit #{pagefirst},#{pagesize};
	</select>
		
	<!-- 전체 게시글 총 수 구하기 -->
	<select id="getTotal" parameterType="hashmap" resultType="_int">
	<choose>
		<when test="consort != '국적'">
			select count(num) from reviewboard where country=#{consort} and title like #{search};
		</when>
		<otherwise>
			select count(num) from reviewboard where title like #{search};
		</otherwise>
	</choose>
	</select>
	
	<!-- 게시글 작성 -->
	<insert id="write" parameterType="reviewboardVO">
	<![CDATA[
	INSERT INTO reviewboard (nickname, title, content, communityname, country, area, introduction, thumbnail, hashtag) values (#{nickname}, #{title}, #{content}, #{communityname} ,#{country}, #{area}, #{introduction}, #{thumbnail}, #{hashtag});
	]]>
	</insert>
	
	<!-- 최근 게시글 번호 조회 -->
	<select id="getMaxNum" resultType="_int">
	<![CDATA[
	select max(num) from reviewboard;
	]]>
	</select>
	
	<!-- 게시글 상세보기 -->
	<select id="viewList" parameterType="_int" resultType="reviewboardVO">
	<![CDATA[
	select * from reviewboard where num = #{contentNum};
	]]>
	</select>
	
	<!-- 임시폴더에서 서버폴더 이동 후 파일 경로 변경 -->
	<update id="contentChange" parameterType="hashmap">
	<![CDATA[
	update reviewboard set content = #{afterContent} where num = ${contentNum};
	]]>
	</update>
	
	<!-- 게시글 상세보기(글 수정 시) -->
	<select id="ModifyList" parameterType="_int" resultType="reviewboardVO">
	<![CDATA[
	select * from reviewboard where num = #{contentnum};
	]]>
	</select>
	
	<!-- 게시글 수정 -->
	<update id="Modify" parameterType="reviewboardVO">
	<![CDATA[
	update reviewboard set title = #{title}, content = #{content}, thumbnail = #{thumbnail}, introduction = #{introduction}, 
		   country = #{country}, area = #{area}, hashtag = #{hashtag} where num = #{num};
	]]>
	</update>	
	
	<!-- 게시글 삭제시 닉네임 체크 -->
	<select id="idCheck" parameterType="_int" resultType="String">
	<![CDATA[
	select nickname from reviewboard where num = #{contentNum};
	]]>
	</select>
	
	<!-- 게시글 삭제 -->
	<delete id="condelete" parameterType="_int">
	<![CDATA[
	delete from reviewboard where num = #{contentNum};
	]]>
	</delete>
</mapper>